---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  author?: string;
  publishDate?: Date;
  category?: string;
  image?: string;
}

const {
  title,
  description,
  author,
  publishDate,
  category,
  image,
} = Astro.props;

const formattedDate = publishDate
  ? new Intl.DateTimeFormat('fr-CA', { year: 'numeric', month: 'long', day: 'numeric' }).format(publishDate)
  : null;
---

<BaseLayout
  title={title}
  description={description}
  image={image}
>
  <article class="max-w-3xl mx-auto">
    {/* Header */}
    <header class="mb-8 lg:mb-12">
      <div class="mb-4">
        {category && (
          <span class="inline-block px-3 py-1 bg-blue-100 text-blue-800 text-xs font-semibold rounded-full">
            {category}
          </span>
        )}
      </div>

      <h1 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
        {title}
      </h1>

      <p class="text-xl text-gray-700 mb-6">
        {description}
      </p>

      {/* Meta Information */}
      <div class="flex flex-wrap items-center gap-4 text-gray-600 text-sm border-t border-b border-gray-200 py-4">
        {author && (
          <div class="flex items-center gap-2">
            <span class="font-medium">Par</span>
            <span>{author}</span>
          </div>
        )}
        {formattedDate && (
          <div class="flex items-center gap-2">
            <span>📅</span>
            <span>{formattedDate}</span>
          </div>
        )}
      </div>
    </header>

    {/* Featured Image */}
    {image && (
      <div class="mb-8 lg:mb-12 rounded-lg overflow-hidden shadow-lg">
        <img src={image} alt={title} class="w-full h-auto" />
      </div>
    )}

    {/* Content */}
    <div class="prose prose-lg max-w-none mb-12">
      <slot />
    </div>

    {/* Footer */}
    <footer class="border-t border-gray-200 pt-8 mt-12">
      <div class="bg-gray-50 rounded-lg p-6">
        <p class="text-gray-700">
          Vous avez des questions suite à cet article ? N'hésitez pas à nous contacter pour plus d'informations.
        </p>
        <a href="/contact/" class="inline-block mt-4 px-5 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
          Nous contacter
        </a>
      </div>
    </footer>
  </article>
</BaseLayout>

<style is:global>
  .prose {
    line-height: 1.75;
  }

  .prose h2 {
    @apply text-2xl font-bold text-gray-900 mt-8 mb-4;
  }

  .prose h3 {
    @apply text-xl font-semibold text-gray-900 mt-6 mb-3;
  }

  .prose p {
    @apply text-gray-700 mb-4;
  }

  .prose ul,
  .prose ol {
    @apply mb-4 pl-6;
  }

  .prose li {
    @apply text-gray-700 mb-2;
  }

  .prose a {
    @apply text-blue-600 hover:text-blue-800 underline transition-colors;
  }

  .prose blockquote {
    @apply border-l-4 border-blue-600 pl-4 text-gray-700 italic my-4;
  }

  .prose code {
    @apply bg-gray-100 rounded px-2 py-1 font-mono text-sm text-gray-800;
  }

  .prose pre {
    @apply bg-gray-900 text-gray-100 p-4 rounded-lg overflow-x-auto mb-4;
  }

  .prose img {
    @apply rounded-lg shadow-md my-6 max-w-full h-auto;
  }
</style>
